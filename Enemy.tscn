extends CharacterBody3D

var health = 100

func take_damage(dmg):
    health -= dmg

    if health <= 0:
        die()

func die():
    GameManager.add_money(50)
    GameManager.add_rep(1)
    queue_free()
